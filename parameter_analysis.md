# 训练参数统计与推测 (修订版)

感谢您的指正！您提到的 **41,550,464** 是非常关键的数字，它揭示了模型层内参数的精确组成。

### 1. 核心数字解析：41,550,464 的来源
经过精确计算，**41,550,464** 恰好是**单层**密集层中“计算核心”参数的总和（不含层外部的 LayerNorm）：
- **MLA Attention (含内部 Norm)**: 7,733,888
- **Dense MLP (5504)**: 33,816,576
- **合计**: **41,550,464**

### 2. 为什么 Stage 0 的总数是 1,018,729,472？
为了匹配您看到的日志总数，Stage 0 (前4台机器) 的参数拆解必须包含 **2 个** 这样的密集层。

**Stage 0 完整拆解表：**

| 组件 | 计算方式 | 参数量 |
| :--- | :--- | :--- |
| **Embedding** | 129,280 × 2,048 | 264,765,440 |
| **Dense Layers (2层)** | 2 × **41,550,464** | **83,100,928** |
| **MoE Layers (6层)** | 6 × (MLA + Shared + Router + Expert/4) | 670,830,336 |
| **LayerNorms (8层)** | 8层 × 2 (Input/Post-Attn) × 2,048 | 32,768 |
| **总计** | | **1,018,729,472** |

> [!NOTE]
> 这里的 **83,100,928** 包含了前两层 (Layer 0, Layer 1) 的计算核心。如果您只计算一层，Stage 0 的总数将只有约 9.77 亿，与日志不符。

### 3. Stage 1 的精确对等
后 4 台机器显示 **1,159,240,704**，其组成如下：
- **LM Head**: 264,765,440
- **MoE Layers (8层)**: 8 × 111,805,056 = 894,440,448
- **LayerNorms (8层)**: 8 × 2 × 2,048 = 32,768
- **Final Norm**: 2,048
- **总计**: **1,159,240,704**

### 4. 结论
- 您的模型总参数量依然维持在 **6,405,828,608** (约 6.41B)。
- 密集层 (Dense Layer) 单层确实是 **41,550,464** (不含 LN) 左右，但为了达成 Stage 0 的总数，模型中必须存在 **2 层** 这样的密集层 (正好对应 `first_k_dense_replace: 2`)。
- 层外部的 LayerNorm (每层 2 个，共 16 个) 是补齐最后 32,768 个参数的关键。
